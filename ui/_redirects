# SPA fallback is handled by Workers assets not_found_handling in wrangler.toml.
# This file is intentionally minimal — do not add /* redirect rules here as
# Cloudflare Workers assets detects them as infinite loops.
